import React from "react";
import { Container } from "../../styled-components";

const Home = () => {
    return (
        <Container>
            <h1>Landing Page</h1>
        </Container>
    );
};

export default Home;
